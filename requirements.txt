# Django REST API
# Python 3.14 requires the Django 5.2 LTS stack (4.2 only supports <=3.12).
Django>=5.2,<6.0
# psycopg3 ships prebuilt wheels for new Python versions (no pg_config build);
# Django 5.2 uses the same "django.db.backends.postgresql" engine for it.
psycopg[binary]>=3.2,<4.0
djangorestframework>=3.16,<3.17
djangorestframework-simplejwt>=5.5,<6.0
django-cors-headers>=4.7,<5.0

# Config / environment
python-dotenv==1.0.1

# NOTE: gohighlevel-api-client (the `highlevel` SDK) was removed -- it was never
# imported; the GHL integration in api/integrations/ghl uses raw `requests`.
# Re-add only if the SDK is actually used.

# Audit / per-entity history
django-simple-history>=3.8,<4.0

# Token encryption at rest (Unite Us credentials)
cryptography>=44,<46

# HTTP
requests>=2.32,<3.0

# Excel (.xlsx) parsing -- CareCircle team roster import (import_unite_us_agents)
openpyxl>=3.1,<4.0

# Production server / static files
gunicorn>=23,<24
whitenoise>=6.8,<7.0

# Object storage (S3): default file storage in production + presigned uploads
# for large import files and future document storage.
boto3>=1.34,<2.0
django-storages>=1.14,<2.0

# Background task queue for async imports + smart diagnostics. Redis is the
# broker/result backend (runs on the same EC2 to start: redis://127.0.0.1).
celery>=5.4,<6.0
redis>=5.0,<6.0

# PDF generation (Nutritionist signed review documents).
reportlab>=4.0,<5.0
